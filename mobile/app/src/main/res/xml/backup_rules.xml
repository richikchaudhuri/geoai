<?xml version="1.0" encoding="utf-8"?>
<!--
    Pre-Android-12 backup rules (API < 31). Mirrors data_extraction_rules.xml
    so the same exclusions apply across OS versions.
-->
<full-backup-content>
    <exclude domain="sharedpref" path="msc_prefs.xml" />
</full-backup-content>
